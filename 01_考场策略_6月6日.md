# 🎯 蓝桥杯国赛 考场策略 — 6月6日

## 你唯一的任务：拿分！不是"做对题"！

---

## ⏱ 4小时时间分配（严格执行）

| 时间 | 做什么 | 目标分数 |
|------|--------|:---:|
| **0-10分钟** | 通读全部题目，在草稿纸上标记"简单/中等/难" | — |
| **10-50分钟** | 干掉填空题 + 2道最简单的编程题 | 30-40分 |
| **50-150分钟** | 主攻中档编程题（会的先做，不会就跳） | 30-40分 |
| **150-210分钟** | 暴力解法补分（每道题都要有代码交上去） | 10-20分 |
| **210-240分钟** | 检查提交 + 没思路的题输出固定值碰运气 | 5分 |

## 🔥 核心原则（写到草稿纸上！）

1. **不看题号顺序，先做性价比最高的**
2. **一道题卡住超过15分钟，立刻跳过**
3. **每道题都要交！哪怕是 print(0) 也好**
4. **不会做的题就暴力搜，暴力不行就输出固定值**
5. **Python递归必加 sys.setrecursionlimit(500000)**
6. **读输入用 sys.stdin.readline，不用 input()**
7. **最后10分钟停止写代码，确认所有题都已提交**

---

## 📊 题目难度识别指南

| 线索 | 难度 | 策略 |
|------|:---:|------|
| n ≤ 20，输入很短 | 简单 | 直接 DFS 暴力/全排列 |
| n ≤ 1000 | 中等 | O(n²) 可过，DP/双循环 |
| n ≤ 10⁵ | 中难 | O(n log n)，贪心/排序/二分 |
| n ≤ 10⁶ | 较难 | O(n)，前缀和/差分/数学 |
| 有"最大值最小"/"最小值最大" | — | **二分答案** |
| 有"最短路径"/"最少步数" | — | **BFS** |
| 有"方案数" | — | **DP** 或 DFS |
| 有图/城市/道路 | — | **Dijkstra** 或 **Floyd** |
| 有"取模 10⁹+7" | — | 答案很大时会说，正常算就行 |
| 有无向图/连通块 | — | **并查集** 或 **DFS** |

---

## 🃏 "骗分"终极方案

### 完全不会做时的输出策略：
```python
# 求最小值 → print(0)
# 求最大值 → print(n) 或 print(10**9)
# 求方案数 → print(1) 或 print(n)
# 求"是否可行" → print("Yes") / print("No")
# 求"最少天数/次数" → print((n + 1) // 2)
# 说"无解输出-1" → 先把暴力写完，跑不过再 print(-1)
```

### 暴力拿部分分的模板：
```python
# 30% 部分分：直接模拟
# 60% 部分分：O(n²) 暴力
# 100%：优化算法

# 举例：求数组中满足条件的对数
# 暴力 O(n²) 能拿前几个测试点的分！
n = int(input())
arr = list(map(int, input().split()))
ans = 0
for i in range(n):
    for j in range(i+1, n):
        if 条件:  # 把题目条件写这里
            ans += 1
print(ans)
```

---

## 📋 考前最后一天（6月5日）做什么

- [ ] 把 `00_必背模板_考前速记.py` 里的模板 **手打3遍**（默写！）
- [ ] 重点默写：DFS + BFS + 背包DP + Dijkstra + 并查集 + 快速幂
- [ ] 做1套真题，严格计时4小时，模拟真实考场
- [ ] 准备好身份证、准考证、笔、水
- [ ] 晚上11点前睡觉

## 🏁 考试当天

- 早餐吃饱，带瓶水
- 提前30分钟到考场
- 开考后第一件事：在每道题的代码里先写 `import sys; sys.setrecursionlimit(500000); input = sys.stdin.readline`
- 填空题如果1分钟没思路，用计算器/Excel/Python暴力跑
- **不要死磕任何一道题！**
